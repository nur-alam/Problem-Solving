#include<cstdio>
using namespace std;
int main(){
    char c[30];
    while(1){
        gets(c);
        //scanf("%[^\t\n]s",c);
        printf("%s\n",c);
         gets(c);
        //scanf("%[^\t\n]s",c);
        printf("%s",c);
    }
    return 0;
}
