#include<stdio.h>
int main()
{
    int i,n,b,a;
    scanf("n",&n);

    for(i=0;i<n;i++){
       scanf("n",&a);
    }

    if(n>0)
    {
         printf("%d",b);
         b++;
    }
    if(n<0)
    {
         printf("%d",b);
         b++;
    }

    return 0;
}
