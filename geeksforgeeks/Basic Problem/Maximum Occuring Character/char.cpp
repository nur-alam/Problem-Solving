#include <stdio.h>

int main() {
	//code
	char ch;
	char ck;
    int t;
    scanf("%d",&t);
    while(t--)
    {
        getchar();
        ch = getchar();
        printf("%d\n",ch);
    }
	return 0;
}
